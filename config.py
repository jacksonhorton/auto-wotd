# /config.py

email = {
    'sender': 'myemail@gmail.com',  # Must be a Gmail unless you change the SMTP server to that of your email.
    'receiver': 'anyemail@anydomain.com',   # This email doesn't have to correspond with the SMTP server below.
    'smtp': 'smtp.gmail.com'    # Must correspond with the 'sender' email
}
pwd = {
    'sender_pwd': 'password-goes-here'
}
